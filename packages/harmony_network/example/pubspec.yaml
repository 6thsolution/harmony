name: example
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # openapi_generator_annotations: ^3.1.3  
  harmony_network:
    path: ../harmony_network 


  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.11.1    

  # openapi_generator: ^3.1.3
  harmony_network_generator:
    path: ../harmony_network_generator 

flutter:
  uses-material-design: true
