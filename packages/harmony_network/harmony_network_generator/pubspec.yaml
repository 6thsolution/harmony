name: harmony_network_generator
description: generator for harmony_network package
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  # flutter: ">=1.17.0"

dependencies:
  # flutter:
    # sdk: flutter
  build: '>=0.12.6 <=2.0.1'
  build_runner: ^1.11.1
  source_gen: ^1.0.0
  path: ^1.8.0
  harmony_network: 
    path: ../harmony_network
  analyzer: '>=1.4.0 <=1.5.0'
  openapi_generator_cli: ^3.1.3

dev_dependencies:
  # flutter_test:
    # sdk: flutter
  pedantic: ^1.11.0
  test: ^1.16.8

# flutter:
